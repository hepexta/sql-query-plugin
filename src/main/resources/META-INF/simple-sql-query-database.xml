<idea-plugin>
    <!--
      Loaded only when the Database Tools plugin (com.intellij.database) is present.
      Kept as a separate descriptor because the main plugin.xml must stay valid in IDEs
      that do not ship Database Tools.
    -->
</idea-plugin>
